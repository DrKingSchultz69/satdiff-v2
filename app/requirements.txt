# Installs the same satdiff package that was evaluated, straight from the repo,
# so the Space cannot drift from the code the metrics were measured on.
git+https://github.com/DrKingSchultz69/satdiff-v2.git

# Deliberately no pins here, and no gradio line at all.
#
# gradio comes from `sdk_version` in the README frontmatter; listing it here
# too makes pip unsolvable. And current gradio already requires
# huggingface_hub>=1.16, starlette>=1.0.1, pydantic>=2.0 — pinning any of them
# below that only recreates the version fights that cost several rebuilds:
# gradio 4.44 wanted HfFolder (gone in hub 1.0), then choked on pydantic
# 2.10's bool additionalProperties, then gradio 5.0.1 hit starlette's new
# TemplateResponse argument order. Let gradio's own constraints resolve.
torchvision
diffusers
pillow
